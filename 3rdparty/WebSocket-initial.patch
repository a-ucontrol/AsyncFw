diff '--color=auto' -Naur WebSocket-orig/WebSocket/WebSocket.cpp WebSocket/WebSocket/WebSocket.cpp
--- WebSocket-orig/WebSocket/WebSocket.cpp	2026-03-27 17:13:36.772935414 +0300
+++ WebSocket/WebSocket/WebSocket.cpp	2026-03-27 17:14:47.087433153 +0300
@@ -10,10 +10,12 @@
 #include "base64/base64.h"
 #include "sha1/sha1.h"
 
-#include <iostream>
+//#include <iostream>
 #include <string>
 #include <vector>
 
+#include <cstring>
+
 using namespace std;
 
 WebSocket::WebSocket() {
@@ -66,7 +68,7 @@
 string WebSocket::trim(string str) 
 {
 	//printf("TRIM\n");
-	char* whitespace = " \t\r\n";
+  char whitespace[] = " \t\r\n";
 	string::size_type pos = str.find_last_not_of(whitespace);
 	if(pos != string::npos) {
 		str.erase(pos + 1);
@@ -88,7 +90,7 @@
 	//UASSERT( theDelimiter.size(), >, 0 );
 	
 	vector<string> theStringVector;
-	int  start = 0, end = 0, length = 0;
+  std::size_t  start = 0, end = 0, length = 0;
 
 	while ( end != string::npos )
 	{
@@ -158,7 +160,7 @@
 	//return WS_OPENING_FRAME;
 }
 
-int WebSocket::makeFrame(WebSocketFrameType frame_type, unsigned char* msg, int msg_length, unsigned char* buffer, int buffer_size)
+int WebSocket::makeFrame(WebSocketFrameType frame_type, const unsigned char* msg, int msg_length, unsigned char* buffer, int /* buffer_size */)
 {
 	int pos = 0;
 	int size = msg_length; 
@@ -223,10 +225,10 @@
 	}
 	else if(length_field == 127) { //msglen is 64bit!
 		payload_length = (
-			(in_buffer[2] << 56) | 
-			(in_buffer[3] << 48) | 
-			(in_buffer[4] << 40) | 
-			(in_buffer[5] << 32) | 
+      ((uint64_t)in_buffer[2] << 56) |
+      ((uint64_t)in_buffer[3] << 48) |
+      ((uint64_t)in_buffer[4] << 40) |
+      ((uint64_t)in_buffer[5] << 32) |
 			(in_buffer[6] << 24) | 
 			(in_buffer[7] << 16) | 
 			(in_buffer[8] << 8) | 
diff '--color=auto' -Naur WebSocket-orig/WebSocket/WebSocket.h WebSocket/WebSocket/WebSocket.h
--- WebSocket-orig/WebSocket/WebSocket.h	2026-03-27 17:13:36.776268747 +0300
+++ WebSocket/WebSocket/WebSocket.h	2026-03-21 13:41:46.776484996 +0300
@@ -56,7 +56,7 @@
 	WebSocketFrameType parseHandshake(unsigned char* input_frame, int input_len);
 	string answerHandshake();
 
-	int makeFrame(WebSocketFrameType frame_type, unsigned char* msg, int msg_len, unsigned char* buffer, int buffer_len);
+  static int makeFrame(WebSocketFrameType frame_type, const unsigned char* msg, int msg_len, unsigned char* buffer, int buffer_len);
 	WebSocketFrameType getFrame(unsigned char* in_buffer, int in_length, unsigned char* out_buffer, int out_size, int* out_length);
 
 	string trim(string str);
